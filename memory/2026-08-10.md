# 2026-08-10 (周日)

## 事件

### ADM多维API上线后遗漏反思

小胡反馈三个严重问题：
1. **ADM Custom分规则指标create报错** — givttotal_imp/clk、sivttotal_basic_imp/clk、sivttotal_advanced_imp/clk、sivttotal_h2_imp/clk 共8个分规则指标，detail接口能正常返回，但create接口传这些token报错
2. **TVM sheetBy限制与界面不一致** — 界面SIVT指标可按活动/按地域分sheet无限制，API有sheetBy=0强制限制
3. **缺少列出历史任务接口** — 用户无法通过API列出自己提交过的任务，只能用status批量查(需提前知道taskId)或detail单个查。PRD第5.6节设计了该接口但技术未实现

古古完成根因分析：
- 问题一根因：case设计了（058~121共30个case），但全部因QA账号缺权限标为⏸️待测，上线时未解决权限阻塞就过了
- 问题二根因：case只按API文档定义的合法组合设计，没有拿界面实际行为做交叉校验
- 问题三根因：PRD第5.6节设计了 `GET /admonitor/v1/drill/tasks` 列历史任务接口，但技术未实现，属于需求交付不完整
- 前两个问题共同根因：测试case设计锚点是"API文档"而非"界面实际行为"
- 问题三本质也是未与界面对齐——界面上有任务列表页，API没有

四步闭环落地：
- AGENTS.md第6条自检升级：从"字段值类型判断需查定义文档"扩展为"基于文档做设计/判断/验证时必须拿实际行为做交叉校验"
- 项目文件新增「上线后遗漏教训」章节，记录根因和防复发措施
- TODO新增4项：报技术侧修复两个bug + 补测权限阻塞case + 补测界面行为对齐case

小胡评价：分析非常棒，核心问题就是"没和界面对齐"

### 本周todo对齐（Octo群聊）

小胡@所有AI对齐本周三条线：
1. 多维API继续测试
2. 芒果SDK流量被判PC问题
3. 三九Agent搭建推进（当前应在指标模型开发阶段 8.1-8.21）

### ADM界面操作测试（Octo单聊）

通过 Puppeteer 自动化操作 ADM QA 环境（omi-api-qa.cn.miaozhen.com），成功进入指标配置第二步页面。活动ID 2507095 填入验证通过。

随后连续报 400001 错误（"参数非法，取值范围 ['text']"），多次重试均失败。小胡问"没钱了吧"——疑似 MLAMP 网关配额问题。

### MLAMP 网关余额查询（飞书单聊）

小胡问模型和余额。查到：
- 网关额度上限 $500
- 已用约 $245.56（usage=24556.4，按分计）
- 剩余约 ~$250
- 当前 API key 后四位 GiaH

注：usage 数字单位未100%确认，以网关后台为准。

### GLM-5.2 连续 400001 报错

Octo 单聊中连续3次报 `400 The request is invalid: ***.***.type 参数非法，取值范围 ['text']`。发生在 puppeteer 浏览器操作之后。可能原因：
- 上下文过长触发模型请求格式问题
- 网关侧参数校验异常
- 不是余额问题（飞书侧同模型正常工作）

## 待办
- [ ] 报技术侧修复两个bug
- [ ] 补测权限阻塞case和界面行为对齐case
- [ ] 确认修复时间线
- [ ] 排查 GLM-5.2 在 Octo session 的 400001 报错根因
