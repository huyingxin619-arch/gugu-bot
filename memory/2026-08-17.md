# 2026-08-17

## 🦞开会群建立 & 分工确定

小胡建了一个 Octo 群「🦞开会」，成员：小胡、octic（小胡的octic）、古古。用于开会协作。

### 最终工作流（小胡确认，8/17更新）
1. octic 会后处理完 → 转写/纪要/待办落盘到 Octo 在线文档
2. octic 用 `octo-cli docs forward-grant` 给 guguhyx_bot 授 reader 权限
3. octic 群里 @古古，给文档链接
4. 古古用 `octo-cli docs content get <docId> --bot-id guguhyx_bot` 读原文和纪要
5. **古古问小胡：这个会议有没有相关文件可以给我**（测试报告、方案文档、数据包等背景文件）
6. 小胡给文件 → 古古结合文件+原文+项目认知，做第一性原理分析
7. 认知不一致的发群里让小胡判断
8. 小胡判完 → 古古更新项目文件

关键点：不依赖本地文件互访，用 Octo 在线文档作为信息传递媒介。背景文件是理解前因后果的必要输入，每个会议都要问小胡要。

### octo-cli 文档读取能力
- `octo-cli docs content get <docId> --bot-id guguhyx_bot` 可读 Octo 在线文档内容
- 需要先 `octo-cli auth login` 建好 bot 的 auth profile（已完成）
- 文档权限由文档创建者通过 `octo-cli docs forward-grant` 授予 reader/writer
- octic 已给 guguhyx_bot 授了 10 份会议文档的 reader 权限
- 文档内容返回 JSON block 结构（ProseMirror/Tiptap 格式），需解析
- `octo-cli docs export <docId> --export-format md -o <path>` 可导出为 markdown

### Octo 群信息查询
- 被问到群名/群信息时，用 `octo-cli` 或 Octo Bot API `GET /v1/bot/groups/:group_no` 查真实群名
- 不要依赖 inbound context 里的 `group_subject`（那是原始群ID）

### 踩坑
- 被问"群名是什么"时直接报了 inbound context 里的 `group_subject`（原始群ID），没去查真实群名。octic 是主动调 `octo_management` 的 group-info 接口查的
- 根因：Octo 插件 inbound.js 里 `GroupSubject` 直接赋的是 `message.channel_id`，没解析群名。这是插件层面的 bug
- 教训：被问到群信息时，主动调 Octo Bot API 查真实群名，不依赖 inbound context

### 8/17会议消化踩坑
- 把TR（request ID）写成了PR，搞混了两个概念
- 在群里直接写了报价具体价格数字，违反脱敏规则
- 归纳太粗，没真正搞懂前因后果就急着出结论（如"数据链需改造"实际是被否掉的）
- 把屏ID和session ID混用
- 人名写错：Darren→Darin
- postpay→postbuy
- alan后续不再负责、CMP对接人换成浪奇，没注意到
- 缺背景文件导致理解偏差——以后每个会议都要问小胡有没有相关文件
- CMP素材计算的具体含义没搞懂就写了进展

---

## core-evolution 周度诊断修复（10:10 心跳执行）

昨晚（8/16 周日 3:30）core-evolution 诊断报告出了14项问题。本次心跳批量修复：
- ✅ #11 修正 AGENTS.md 触发词表4个错误路径
- ✅ #12 修正 overview.md 多维钻取API路径
- ✅ #10 补全 AGENTS.md 触发词表缺失的7条触发词
- ✅ #8 更新 USER.md 项目与关注点——KA品牌安全已结束
- ✅ #9 补全 MEMORY.md adm实例模型信息

## core-evolution 剩余修复（11:39 小胡确认后执行）

小胡看完诊断报告后确认全部修改：
- ✅ #1/#2 AGENTS.md 自检清单上限从5调到10，注解修正为8条
- ✅ #3 MEMORY.md 备份机制去重 → 一行摘要指向 TOOLS.md
- ✅ #4 MEMORY.md 用户身份去重 → 一行摘要指向 USER.md
- ✅ #5 客户脱敏规则去重 → MEMORY.md 为唯一数据源，SOUL.md/AGENTS.md 改为指向
- ✅ #6 安全规则去重 → SOUL.md 精简凭证细节
- ✅ #13 adm-fields-reference.txt 补入 MEMORY.md 索引
- ✅ #14 四个已结束项目加 ✅已归档 标记，保留原位不迁移路径

涉及文件：AGENTS.md、MEMORY.md、SOUL.md、overview.md、ka-brand-safety.md、ant-ivt-research.md、tag-audit.md、admonitor-bot-dashboard.md

## octo-daemon 安装配置（13:05）

小胡要求安装 octo-daemon 运行时。步骤：
1. ✅ 安装 v1.1.0（darwin-arm64）
2. ✅ 登录认证（token认证，server: im.deepminer.com.cn）
3. ✅ daemon启动 — 默认端口19514被 huangchunbo 的 multica daemon 占用，用 `--profile gugu` 隔离配置启动在独立端口
4. ✅ 安装内置skill octo-loop — 装到 gugu 和 adm 两个实例的 skills 目录

注意：profile=gugu 的 daemon 配置隔离于默认 profile，需要用 `octo-daemon --profile gugu` 操作

## 需求审核Skill讨论（13:37-15:14）

群成员：小胡、AdMonitor、adm PM助手、古古

### 讨论过程
小胡发起需求：搞一个需求审核skill，adm提供专业知识，PM助手提供Loop背景和skill构成，古古补充

### 关键决策
1. **skill定位**：不是自动审核工具，是帮产品梳理清楚需求再提给技术的辅助工具
2. **分类体系**：5类——Bug/数据异常/新功能/二次开发（跑数需求）/其他（含咨询）
3. **模块清单**：从最初的混级清单→AdMonitor按功能域分→小胡调整为按架构视角分（ADM/TVM分开，系统层面：界面/API-MCP/大数据/SDK/SNAP/CT/实验室）
4. **v2/v3合并**：小胡要求一步到位，不分阶段迭代。AdMonitor知识粗判+工程风险审核都做进这版
5. **工程风险5维度**：界面回显对齐/权限适配/数据逻辑对齐/异步工程风险/边界场景（条件触发，信息不够时跳过）
6. **通用必填项**：需求标题/需求描述/需求方/涉及产品线/涉及系统层面/优先级。短路逻辑只有需求标题+需求描述缺失才打回
7. **Step 5标准需求格式**：审核通过后自动转化为标准格式，产品确认后直接提给技术

### 最终流程
需求描述输入→Step1分类→Step2信息完整性检查→Step3 AdMonitor知识粗判→Step4工程风险提示→Step5转化为标准需求格式→回产品确认→提给技术

### 依赖（均已就绪）
- 5份references文件（指标定义/API能力/字段/约束/工程风险清单）

### 落地状态
- PM助手已落地skill到 skills/requirement-review/ 目录
- 已git commit
- 包含SKILL.md + 5份references
- 小胡确认后加了Step 5（标准需求格式转化）、通用项加需求标题去期望结果、TVM大数据加PMO

### 我的贡献
- 分类体系补了"数据异常"和"二次开发（跑数需求）"两类
- 通用项去期望时间改优先级、加涉及模块
- 工程风险5维度清单
- v2/v3合并建议
- 模块清单两层结构建议→AdMonitor优化为架构视角

### 研发scope映射（PM助手提供）
- 前端/后端→吕金果（二次分配黄春波、李晴晴）
- 大数据→张成（二次分配：董文菁/于长亮/陈龙/杨春雪/陈鹏/吴坤城）
- MCP/lite/海外→赵思捷
- 端侧→周康平
- SDK→张乾（Android）、王立涛（iOS）
